{
  "name": "wolf-equipment-catalogue",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-catalogue.mjs --skip-images",
    "import": "node scripts/build-catalogue.mjs --download-images",
    "refresh-images": "node scripts/build-catalogue.mjs --download-images --refresh-images",
    "serve": "python3 -m http.server 8080"
  },
  "optionalDependencies": {
    "sharp": "^0.34.0"
  },
  "engines": {
    "node": ">=20"
  }
}
